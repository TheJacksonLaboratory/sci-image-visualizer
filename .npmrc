# Reject an unsupported Node up front rather than in the middle of a Vite build.
# Without this, `engines` in package.json is advisory: Node 16 gets as far as
# `crypto.getRandomValues is not a function` from inside a dependency, which says
# nothing about the actual cause.
engine-strict=true
