# Sample images for examples/browser-image are stored via Git LFS.
# Scoped to this path so the repo's other PNGs (docs/, src/lib/assets/) stay in git.
examples/browser-image/sample-images/** filter=lfs diff=lfs merge=lfs -text
